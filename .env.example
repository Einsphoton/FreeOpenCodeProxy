PORT=3000
DATA_DIR=./data
# Optional defaults; can also be configured in the Settings page.
UPSTREAM_BASE_URL=https://api.openai.com/v1
UPSTREAM_API_KEY=
# Optional API key required by clients calling this proxy.
PROXY_API_KEY=
